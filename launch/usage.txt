# ============================================================
# real-robot-deployment — single robot, 8 fruits, both aisles
# ============================================================

# Terminal 1
roscore

# Terminal 2 — full launch (real robot)
roslaunch rbkairos_etf_services bringup_real.launch

# Terminal 2 — dry-run (planner only, no action server calls)
roslaunch rbkairos_etf_services bringup_real.launch dry_run:=true

# Override instance file
roslaunch rbkairos_etf_services bringup_real.launch instance_name:=instance_new_real.rddl


# ============================================================
# SIM-integration — dual robot simulation
# ============================================================

# Launch both robots independently (recommended)
./launch/bringup_sim_multi.sh
./launch/bringup_sim_multi.sh dry_run:=false

# Launch robots individually
roslaunch rbkairos_etf_services robot1_sim.launch dry_run:=true
roslaunch rbkairos_etf_services robot2_sim.launch dry_run:=true
